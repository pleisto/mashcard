import React from 'react'
import { Icon, IconProps } from '../Icon'

export const FontSize: React.FC<IconProps> = props => {
  return (
    <Icon {...props}>
      <iconpark-icon name="font-size" />
    </Icon>
  )
}
