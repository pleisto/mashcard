import React from 'react'
import { Icon, IconProps } from '../Icon'

export const ListOrdered: React.FC<IconProps> = props => {
  return (
    <Icon {...props}>
      <iconpark-icon name="list-ordered" />
    </Icon>
  )
}
