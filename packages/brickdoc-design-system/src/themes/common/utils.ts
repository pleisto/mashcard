export const commonUtils = {
  boxSize: (val: string) => ({
    width: val,
    height: val
  }),
  marginX: (val: string) => ({
    marginLeft: val,
    marginRight: val
  }),
  marginY: (val: string) => ({
    marginTop: val,
    marginBottom: val
  }),
  paddingX: (val: string) => ({
    paddingLeft: val,
    paddingRight: val
  }),
  paddingY: (val: string) => ({
    paddingTop: val,
    paddingBottom: val
  })
}
