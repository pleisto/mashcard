import { theme } from '@brickdoc/design-system'

export const variants = {
  active: {
    true: {
      backgroundColor: theme.colors.secondarySelected
    }
  },
  disabled: {
    true: {
      backgroundColor: theme.colors.iconDisable
    }
  }
}
