import { ExtensionMeta } from '../../common'

export interface DropBlockOptions {}

export interface DropBlockAttributes {}

export const meta: ExtensionMeta = {
  name: 'dropBlock',
  extensionType: 'extension'
}
