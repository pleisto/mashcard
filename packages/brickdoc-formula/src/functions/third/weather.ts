import { ContextInterface, FunctionClause } from '../..'

export const META_LOCATION = async (ctx: ContextInterface, latt: number, long: number): Promise<number> => {
  return 0
}

export const THIRD_WEATHER_CLAUSES: FunctionClause[] = []
