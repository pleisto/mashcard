{
  "name": "@brickdoc/dotenv",
  "version": "0.1.0",
  "description": "Smart load environment variables from a .env file.",
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "dependencies": {
    "dotenv": "^16.0.0",
    "dotenv-expand": "^8.0.3"
  },
  "sideEffects": false,
  "devDependencies": {
    "@types/cross-spawn": "^6.0.2"
  },
  "scripts": {
    "lint": "yarn lint:eslint && yarn lint:type",
    "lint:eslint": "TIMING=1 run -T eslint --cache --ext ts,tsx src",
    "lint:fix": "TIMING=1 run -T eslint --cache --ext ts,tsx src --fix",
    "lint:type": "run -T tsc"
  }
}
