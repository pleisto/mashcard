{
  "name": "@brickdoc/nestjs-redis",
  "version": "0.1.0",
  "description": "node-redis module for nestjs",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brickdoc/brickdoc-ce.git"
  },
  "bugs": {
    "url": "https://github.com/brickdoc/brickdoc-ce/labels/active-support"
  },
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "scripts": {
    "lint": "yarn lint:eslint && yarn lint:type",
    "lint:eslint": "TIMING=1 run -T eslint --ext ts,tsx src",
    "lint:fix": "TIMING=1 run -T eslint --ext ts,tsx src --fix",
    "lint:type": "run -T tsc",
    "test:jest": "run -T jest"
  },
  "dependencies": {
    "redis": "^4.0.4"
  },
  "sideEffects": false,
  "devDependencies": {
    "@nestjs/common": "^8.4.3",
    "@nestjs/core": "^8.4.3",
    "@nestjs/testing": "^8.4.3"
  },
  "peerDependencies": {
    "@nestjs/common": "^8",
    "@nestjs/core": "^8"
  }
}
