{{- $fullName := include "brickdoc.fullname" . -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "brickdoc.labels" . | nindent 4 }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
        {{- if .Values.previewEnv }}
        - path: /devtools/mailer
          pathType: Prefix
          backend:
            service:
              name: {{ include "brickdoc.devDependenciesService" . }}
              port: 
                number: 8025
        {{- end }}
        - path: /
          pathType: Prefix
          backend:
            service:
              name: {{ $fullName }}
              port: 
                number: 3000